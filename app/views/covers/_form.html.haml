= simple_form_for @cover, url: project_cover_path do |f|
	= f.input :photographer, as: :string
	= f.input :license, as: :string
	.well
		.row.text-center
			= f.button :submit, class: 'btn-primary btn-well'
			= yield